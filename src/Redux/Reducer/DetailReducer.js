let initialState = {
  DetailProduct: {},
};

export default function DetailReducer(state = initialState, action) {
  const {type, payload} = action;
  switch (type) {
    default:
      return state;
  }
}
